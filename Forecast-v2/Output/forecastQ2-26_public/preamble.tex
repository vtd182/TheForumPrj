% ============================================================
% PREAMBLE — FORECAST 2026
% ============================================================

\documentclass[12pt, a4paper, openany]{book}

% --- Encoding & Vietnamese ---
\usepackage{fontspec}
\newfontfamily\ipafont{Arial Unicode MS} % Universal coverage for IPA symbols
\usepackage[vietnamese]{babel}

% --- Page Geometry ---
\usepackage[
  top=2cm,
  bottom=2cm,
  left=2.5cm,
  right=2cm,
  headheight=16pt
]{geometry}

% --- Math ---
\usepackage{amsmath, amssymb, amsfonts, mathtools}

% --- Graphics & Colors ---
\usepackage{graphicx}
\graphicspath{{images/}}
\usepackage[table, dvipsnames, svgnames, x11names]{xcolor}
\usepackage{tikz}
\usetikzlibrary{
  calc,
  positioning,
  shadows,
  decorations.pathreplacing,
  decorations.markings,
  arrows.meta,
  patterns,
  intersections,
  through,
  angles,
  quotes,
  3d,
  perspective
}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% --- Tables ---
\usepackage{tabularx, booktabs, array, multirow, multicol}
\usepackage{colortbl}

% --- Custom Columns ---
\newcolumntype{V}[1]{>{\raggedright\arraybackslash\small}m{#1}}
\newcolumntype{C}[1]{>{\centering\arraybackslash\small}m{#1}}
\newcolumntype{I}[1]{>{\centering\arraybackslash\ipafont\small}m{#1}}
\newcolumntype{Y}{>{\raggedright\arraybackslash\small}X}

\raggedbottom

% --- Lists ---
\usepackage{enumitem}

% --- tcolorbox (for styled boxes) ---
\usepackage[most, skins, breakable]{tcolorbox}

% --- Header & Footer ---
\usepackage{fancyhdr}

% --- Hyperlinks ---
\usepackage[
  colorlinks=true,
  linkcolor=ForumDarkBlue,
  urlcolor=ForumOrange,
  citecolor=ForumGreen
]{hyperref}

% --- Misc ---
\usepackage{microtype}
\usepackage{titlesec}
\usepackage{etoolbox}
\usepackage{pgffor}
\usepackage{calc}

% ============================================================
% IMPORT MODULES
% ============================================================

% Bảng màu
\input{styles/colors}

% Chapter & Section styling
\titleformat{\chapter}[display]
  {\normalfont\Huge\bfseries\color{ForumFreshBlue}}
  {\chaptertitlename\ \thechapter}
  {20pt}
  {\Huge}
\titlespacing*{\chapter}{0pt}{-30pt}{20pt}

\titleformat{\section}
  {\normalfont\Large\bfseries\color{ForumFreshBlue}}
  {\thesection}{1em}{}
\titleformat{\subsection}
  {\normalfont\large\bfseries\color{ForumFreshMid}}
  {\thesubsection}{1em}{}

% Header & Footer
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE]{\small\textit{\leftmark}}
\fancyhead[RO]{\small\textit{The Forum Center}}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[C]{\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

\let\cleardoublepage\clearpage

% Tcolorbox environments
\input{styles/environments}

% Custom commands
\input{styles/commands}

% --- IELTS Speaking Custom Environments ---

% Part 1 & Part 3: Lightweight question indicator (left blue bar + subtle bg)
\newtcolorbox{ieltsquestion}{
  enhanced,
  breakable,
  frame hidden,
  borderline west={3pt}{0pt}{ForumFreshBlue},
  colback=ForumFreshLight,
  arc=0pt,
  top=6pt, bottom=6pt, left=10pt, right=8pt,
  before skip=8pt,
  after skip=6pt
}

% Part 2: Cue Card prompt — matches reference image with title tab
\newtcolorbox{ieltsprompt}[1][]{
  enhanced,
  breakable,
  boxrule=0.8pt,
  colframe=ForumFreshBlue!80,
  colback=ForumFreshLight,
  arc=3mm,
  top=8pt, bottom=8pt, left=10pt, right=10pt,
  fonttitle=\bfseries\sffamily\small,
  coltitle=white,
  colbacktitle=ForumFreshBlue!80,
  title={IELTS Part 2 — Cue Card},
  attach boxed title to top left={yshift=-2mm, xshift=6mm},
  boxed title style={
    sharp corners=east,
    arc=2pt,
    boxrule=0pt,
    left=6pt, right=6pt, top=3pt, bottom=3pt
  },
  before skip=12pt,
  after skip=8pt
}

% Box for Model Answers (specifically Part 2)
\newtcolorbox{ieltsanswer}{
  enhanced,
  breakable,
  colframe=ForumFreshBlue!25,
  colback=white,
  boxrule=0.6pt,
  arc=2pt,
  left=12pt, right=12pt, top=10pt, bottom=10pt,
  before skip=4pt,
  after skip=12pt,
  borderline west={2pt}{0pt}{ForumFreshBlue!50}
}

% Part 3: Discussion Questions header
\newcommand{\partthreeheader}{%
  \vspace{10pt}%
  \noindent\textcolor{ForumFreshBlue}{\rule{\linewidth}{1.2pt}}%
  \vspace{4pt}%
  \noindent{\large\bfseries\sffamily\textcolor{ForumFreshBlue}{IELTS Part 3 — Discussion Questions}}%
  \vspace{6pt}%
}

% Separator between Q&A blocks
\renewcommand{\separator}{%
  \vspace{6pt}%
  \noindent\makebox[\linewidth]{\textcolor{ForumFreshBlue!20}{\rule{0.6\linewidth}{0.6pt}}}%
  \vspace{6pt}%
}


